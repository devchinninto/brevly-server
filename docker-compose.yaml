services:
  pg-test:
    image: postgres:17-alpine
    container_name: brevly-server-test
    ports:
      - '5433:5433'
    env_file:
      - '.env.test'

  pg:
    image: postgres:17-alpine
    container_name: brevly-server
    ports:
      - '5432:5432'
    env_file:
      - '.env'
